# Example .env file
# Copy this file to .env and configure your settings

# LLM API keys are stored in: ~/.lex_mint/keys_config.yaml
# (managed by Settings -> Providers, not environment variables)

# API Configuration
API_HOST=0.0.0.0
API_PORT=8988
FRONTEND_PORT=5173
# Frontend API URL is derived from API_PORT (single source of truth)
# FRONTEND_PORT is used by start.bat/start_frontend.* scripts.
# Worktree example: API_PORT=8901, FRONTEND_PORT=5181

# CORS Configuration
# Keep this list in sync with FRONTEND_PORT when using a custom port.
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://localhost:3000"]

# Storage Configuration
CONVERSATIONS_DIR=conversations

# Optional: Server-side directory picker roots for Projects
# Comma-separated absolute or relative paths (relative to backend working dir)
# PROJECTS_BROWSE_ROOTS=.,/Users/you/code

# Logging
LOG_LEVEL=INFO

# Optional: LangSmith for tracing
# LANGCHAIN_API_KEY=your_langchain_api_key_here
# LANGCHAIN_TRACING_V2=true
